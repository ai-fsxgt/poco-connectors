diff --git a/internal/helpers/aitable.go b/internal/helpers/aitable.go
index 0f9da4e..2c268e3 100644
--- a/internal/helpers/aitable.go
+++ b/internal/helpers/aitable.go
@@ -943,7 +943,7 @@ func suggestOperator(op string) string {
 // 对网络瞬断、服务端 5xx、retryable:true 的错误自动指数退避重试，
 // 同时解决 CI 偶发失败和线上用户关键链路被阻断的问题。
 
-const aitableMaxRetries = 3
+const aitableMaxRetries = 0
 
 // callAitableTool 是 aitable 专用的 MCP 调用入口，带自动重试。
 // 替代直接调用 callMCPTool，对网络抖动和服务端瞬态错误进行透明重试。
diff --git a/internal/localio/upload.go b/internal/localio/upload.go
index 5a9f034..07769e5 100644
--- a/internal/localio/upload.go
+++ b/internal/localio/upload.go
@@ -92,7 +92,7 @@ func putFileWithClient(ctx context.Context, rawURL, path string, maxBytes int64,
 	}
 	result := UploadResult{SizeBytes: info.Size()}
 	var lastErr error
-	for attempt := 1; attempt <= 3; attempt++ {
+	for attempt := 1; attempt <= 1; attempt++ {
 		result.Attempts = attempt
 		reader := io.NewSectionReader(file, 0, info.Size())
 		request, requestErr := newUploadRequest(ctx, http.MethodPut, rawURL, reader)
@@ -114,7 +114,7 @@ func putFileWithClient(ctx context.Context, rawURL, path string, maxBytes int64,
 		} else {
 			lastErr = callErr
 		}
-		if attempt < 3 {
+		if attempt < 1 {
 			timer := time.NewTimer(time.Duration(attempt) * 200 * time.Millisecond)
 			select {
 			case <-ctx.Done():
diff --git a/internal/transport/client.go b/internal/transport/client.go
index f78fd0e..5b656e4 100644
--- a/internal/transport/client.go
+++ b/internal/transport/client.go
@@ -63,7 +63,7 @@ const (
 	defaultHTTPTimeout = 30 * time.Second
 
 	// Default retry parameters for JSON-RPC calls.
-	defaultMaxRetries    = 1
+	defaultMaxRetries    = 0
 	defaultRetryDelay    = 500 * time.Millisecond
 	defaultRetryMaxDelay = 5 * time.Second
 

